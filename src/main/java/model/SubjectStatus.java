package model;

public enum SubjectStatus {
    // ? Still thinking what of these to select as default
    NULL,
    NOT_STARTED,

    // ? 
    APPROVED,
    IN_PROGRESS,
    DROPPED,
    FAILED,
    OTHER

}
