package model;

public enum AcademicArea {
    // ? default
    NULL,
    PENDING_TO_CATALOG,

    // ? academic areas
    MATH,
    PHYSICS,
    CHEMISTRY,
    COMPUTER_SCIENCE,
    ENGINEERING,
    OTHER
}
